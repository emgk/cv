import React from 'react';

const Footer = () => {
  return (
    <footer className="border-t mbs-10 border-t-cv-timeline-stale text-center px-10 py-10">
      <p className="text-sm text-cv-text">
        This website is fully handmade with Next.JS (SSR) + Drizzle + PosgreSQL + TailwindCSS +
        TypeScript
      </p>
    </footer>
  );
};

export default Footer;
